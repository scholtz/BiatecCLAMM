# Function: clammSendOnlineKeyRegistrationSender()

> **clammSendOnlineKeyRegistrationSender**(`input`): `Promise`\<`string`\>

Biatec can execute the online key registration to protect algorand network with algos deposited in the lp pool

## Parameters

### input

`IClammRemoveLiquidityInput`

## Returns

`Promise`\<`string`\>

txid
