# Function: clammSwapSender()

> **clammSwapSender**(`input`): `Promise`\<`string`\>

Swap asset a or asset b at the biatec concentrated liquidity amm

## Parameters

### input

`IClammSwapInput`

## Returns

`Promise`\<`string`\>

txid
